# virtio_scsi: check get_parent_node / get_driver in ctor

Only `VirtioSCSIController.cpp`.

`register_device` is `virtio-scsi-register-get-driver/`.
Independent apply. Same topic `virtio-scsi` on Gerrit, new Change-Id.
