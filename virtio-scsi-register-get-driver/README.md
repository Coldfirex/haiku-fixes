# virtio_scsi: check get_driver in register_device

Only `virtio_scsi.cpp` `virtio_scsi_register_device`.

Ctor path is `virtio-scsi-controller-get-driver/`.
Apply independently. Same Gerrit topic (`virtio-scsi`); separate Change-Ids.
